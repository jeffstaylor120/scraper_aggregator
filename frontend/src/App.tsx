// frontend/src/App.tsx — Single compact newsletter page: templates + runs + feedback.
import { NewsletterPage } from './pages/NewsletterPage'
import './App.css'

function App() {
  return <NewsletterPage />
}

export default App
